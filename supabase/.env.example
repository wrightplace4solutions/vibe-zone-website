# Stripe Secret Keys (NEVER commit these to git!)
# Get these from https://dashboard.stripe.com/apikeys
# Note: This is an EXAMPLE file with placeholder keys - not actual secrets
# stripe-lint-disable-next-line
STRIPE_SECRET_KEY=sk_test_your_secret_key

# Stripe Webhook Secret (for webhook verification)
# Get this from https://dashboard.stripe.com/webhooks after creating a webhook
# stripe-lint-disable-next-line
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
